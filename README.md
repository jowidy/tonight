# Tonight

A small recipe selector for deciding what to cook, designed for phones and desktops.

This repository contains the browser application. Recipe data is published separately as AES-256-GCM ciphertext and requires a private sharing link. The decryption key is never stored in this repository or sent to the hosting server.

Preferences last only for the current tab. No accounts, analytics, or permanent taste profiles.
